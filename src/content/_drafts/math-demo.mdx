---
title: 'LaTeX, code, and figures — a demo post'
description: 'Sanity-check post exercising KaTeX, syntax-highlighted code, and an image embed.'
pubDate: 'May 27 2026'
heroImage: '../../assets/blog-placeholder-3.jpg'
---

A quick tour of the building blocks this site supports.

## Inline and display math

Euler's identity, inline: $e^{i\pi} + 1 = 0$. And as a display equation:

$$
\int_{-\infty}^{\infty} e^{-x^2}\, dx = \sqrt{\pi}
$$

A multi-line aligned environment also works:

$$
\begin{aligned}
\nabla \cdot \mathbf{E} &= \frac{\rho}{\varepsilon_0} \\
\nabla \cdot \mathbf{B} &= 0 \\
\nabla \times \mathbf{E} &= -\frac{\partial \mathbf{B}}{\partial t} \\
\nabla \times \mathbf{B} &= \mu_0 \mathbf{J} + \mu_0 \varepsilon_0 \frac{\partial \mathbf{E}}{\partial t}
\end{aligned}
$$

## Code blocks

```python
import numpy as np

def gaussian(x, mu=0.0, sigma=1.0):
    return np.exp(-0.5 * ((x - mu) / sigma) ** 2) / (sigma * np.sqrt(2 * np.pi))
```

## Figures

Use the standard Markdown image syntax for pre-rendered plots and diagrams:

![Placeholder figure — replace with a Matplotlib/Manim export](../../assets/blog-placeholder-2.jpg)

For animations, drop in an `<video>` tag pointing at an MP4 you exported from Manim or similar.
